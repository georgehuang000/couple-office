# 情侣 OA：完整产品与技术实施方案

**版本：V1.0｜方案日期：2026-09-15｜项目代号：couple-oa**

产品暂名：**两人事务所**。空间内部可自定义为“两人有限公司”等昵称；正式小程序名称需另行核名、审核。本文件是产品与开发规格，不代表代码已经实现、云环境已经开通或小程序已经通过审核。

## 01｜方案摘要与范围

### 1.1 一句话定位

为两个人提供一个共同处理生活事项的微信小程序：把约定、待办、日程、愿望和轻量审批放在同一处。保留 OA 的仪式感，但不把亲密关系变成考勤、监控或绩效管理。

已确认方向为微信小程序、CloudBase 云开发、不自行购买和维护云服务器。以下是本方案采用的默认值：先两人自用、个人主体、中文界面、暂不收费、暂不向陌生人开放注册。正式可用服务类目、认证要求和消息模板须在实际账号后台确认。

### 1.2 最终技术决策

| 项目 | 本方案决策 |
| --- | --- |
| 客户端 | 微信小程序原生 WXML / WXSS / TypeScript |
| 后端 | CloudBase 云函数，前台业务集中在一个 api 函数内按模块组织 |
| 身份 | 原生 wx.cloud 调用链；服务端读取可信微信调用上下文 |
| 数据库 | CloudBase 文档型数据库；不同时引入 MySQL / PostgreSQL |
| 文件 | 首版使用预设头像，不做相册；备份文件受限保存 |
| 部署 | 微信开发者工具 + 云开发控制台；自动化部署后续再加 |
| 提醒 | 站内消息必做；微信订阅消息作为有条件可用的增强 |
| 维护 | 成本告警、数据导出、备份恢复、权限回归测试 |

上述托管能力和原生云调用路径有官方文档支持；“免自建服务器”不等于“免开发后端、免鉴权或免费用”。[S1][S2]

### 1.3 两个交付节点

**V0.1 可用闭环：**可信身份、双人绑定、工作台、待办、审批、站内消息、设置，以及最小的数据导出和解绑能力。先让两台真机上的两个人真实协作。

**V1.0 完整首版：**在上述基础上加入共同日历、纪念日、愿望清单；完成隐私指引、备份恢复、成本控制与上线检查。订阅消息须经过能力验证，缺少适配模板时不阻塞核心业务。

**明确暂不做：**相册、视频、聊天、支付与转账、复杂记账、AI 助理、自动定位、强制打卡、情绪评分、多级审批、积分商城、公开社区、其他客户端和通用后台管理平台。

## 02｜开工前必须核实的平台条件与费用

### 2.1 先纠正三个容易误解的地方

第一，不买服务器是不用单独购买 CVM 并维护 Linux、数据库进程和部署环境；云函数、数据库、存储仍然是后端，也仍需配置权限和备份。[S1][S2]

第二，不应承诺免费体验环境能够长期承载正式小程序。2026-09-15 查阅的 CloudBase 资源点价格文档列明：免费体验版每月 3,000 资源点；小程序发布后，相关免费体验环境到期时间变更为上线后的第 15 天，后续存在停服隔离及销毁风险。微信侧体验环境与云开发控制台体验环境的续期政策也有区别，实际环境以后台为准。[S3]

第三，后续接入 AI 或用户增长，不必然要求自购服务器。CloudBase 还提供云托管等托管能力；是否改架构应由实际运行限制、成本或可迁移性决定，而不是预先为增长建设复杂基础设施。[S1]

### 2.2 开发前的能力确认单

| 确认项 | 完成证据 / 处理方式 |
| --- | --- |
| 个人主体服务类目 | 后台存在与实际待办、日历、生活协作功能匹配的类目；不得用无关类目规避审核 |
| AppID | 取得一个正式小程序 AppID；本项目不需要按模块注册多个 AppID |
| 云环境关联 | 小程序与环境正确关联，真机成功调用云函数并识别自身身份 |
| 数据库类型 | 选择支持文档型数据库和目标小程序接入路径的环境，不误选其他数据库模式 |
| 套餐与有效期 | 记录计费模式、月费、续费价、到期日、超额计费开关及告警方式 |
| 备案与认证 | 按小程序后台当前要求完成对应手续；注册、认证、备案、代码审核不是同一件事 |
| 订阅消息 | 实际类目是否提供适配模板，能否授权并成功发送给另一台真机 |
| 双端兼容 | 至少用一台 iPhone 和一台 Android 真机验证核心流程；设备不足时标明缺测 |

### 2.3 预算口径

官方目前列示个人版 **19.9 元/月，为限时优惠价格**，包含每月 40,000 资源点。不要据此承诺永久价格，也不能把套餐费用当成总费用上限。[S3]

建议为“一个正式环境、两个人、文本数据为主、不用 AI 和相册”的方案先预留 **30–50 元/月**运行预算。这是项目预留额，不是平台报价或成本保证；另计实际发生的认证、额外环境、超额资源等费用。按 19.9 元连续支付 12 个月计算，仅套餐部分为 238.8 元，这只是当前单价不变情况下的算术示例。

开发阶段优先利用账号实际可领取的体验权益；正式使用前选择可持续的付费环境。设置用量告警，审慎开启按量付费。告警可能有统计延迟，不能替代接口限流和业务上限。[S3]

## 03｜信息架构与页面规格

### 3.1 四个底部导航

**工作台｜事项｜日历｜我们**。审批不单占一个底部 Tab，而是放在“事项”的二级切换中；个人设置收进“我们”，减少空页面。

| 页面 | 主要内容 | 核心操作 |
| --- | --- | --- |
| 欢迎 / 身份初始化 | 产品介绍、隐私说明、创建或加入入口 | 进入自己的空间，或查看明确标注的演示 |
| 空间创建 / 邀请 | 空间名称、在一起日期、邀请码、加入申请 | 创建、复制邀请、确认另一名成员 |
| 工作台 | 今天待办、待处理申请、最近日程、纪念日 | 快速新建，进入事项详情 |
| 事项列表 | 待办 / 审批切换；与我有关 / 全部 | 筛选、新建、打开详情 |
| 待办详情 / 编辑 | 标题、说明、负责人、截止时间、状态 | 编辑、完成、重开、取消 |
| 审批详情 / 发起 | 固定表单、申请内容、审批意见、时间线 | 提交、同意、再商量、撤回 |
| 日历 / 日程详情 | 月历、所选日期的日程、纪念日 | 新建、调整日期、查看安排 |
| 我们 / 愿望清单 | 两人信息、恋爱天数、愿望、设置入口 | 新建愿望、标记实现、查看设置 |
| 消息中心 | 站内未读与已读消息 | 进入相关记录、标为已读 |
| 设置与数据管理 | 称呼、预设头像、时区、提醒、导出、解绑 | 更新本人设置、管理数据 |

### 3.2 工作台内容优先级

首页先回答“今天我们有什么要处理”，再展示装饰性统计。排列为：空间名称与问候 → 今日待办和待处理审批 → 最近三条日程 → 最近纪念日 → 最近共同动态。

“在一起多少天”根据双方设置的日期计算，不用假数据填充。没有待办时显示“今天暂时没有安排，记一件想一起做的事吧”，而不是空表格。所有统计均来自真实记录，不做“关系健康度”等没有依据的分数。

### 3.3 统一交互要求

列表页必须具备加载、空白、失败、无权限四种状态；编辑页提交中禁用重复点击，失败保留输入。危险操作须有确认和明确后果说明。成功提示以服务端写入结果为准，不能只改页面后显示“保存成功”。

V0.1 没实现日历、愿望时，不显示不能使用的正式入口。开发者工具可通过开发配置显示占位页，但不得将其作为完成功能交付。

## 04｜身份识别、邀请绑定与成员规则

### 4.1 身份识别

采用微信原生云开发链路：客户端调用 wx.cloud.callFunction，云函数使用 wx-server-sdk 的 getWXContext 读取 OPENID / APPID。客户端提交的 openid、user_id、couple_id 均不能作为鉴权依据。[S2]

服务端为可信身份建立 users 记录并返回业务 user_id。用户自行填写昵称，头像使用预设图标；首版不索取手机号，不假定能够自动取得微信头像、真实姓名或昵称。

本方案采用一个固定的、经过实测的小程序接入路径；不把 wx.cloud 原生调用与另一个自定义登录体系混用。[S2]

### 4.2 创建与绑定流程

流程为：A 创建空间 → A 生成邀请 → B 输入邀请并提交加入申请 → A 核对 B 的称呼及确认提示 → A 确认 → 两人进入同一空间。B 提交申请后不能立即读取 A 的任何业务数据。

邀请码不是固定的“520131”。采用安全随机生成的 10 位易读字母数字码，排除易混淆字符；数据库只存验证码哈希。默认有效 24 小时，重新生成即使旧码失效，完成绑定后作废。邀请码仅在生成时显示；遗失就重新生成，不提供明文找回。

设定可配置的防滥用限制：每个用户在 15 分钟内最多尝试 5 次错误邀请码；错误、过期、撤销等对外统一提示，避免泄露空间信息。邀请确认前不能返回空间中的待办、成员联系方式或历史内容。

### 4.3 原子性规则

确认绑定必须在同一数据库事务中验证并更新：邀请仍有效、申请人未绑定其他活跃空间、确认者是邀请发起人、空间仍在等待成员、总人数不会超过两人。将用户、空间、邀请和加入申请一起更新；失败时全部回滚。

事务内不发送微信消息。事务成功后再尝试外部通知。并发测试中即使两个加入申请同时被处理，也只能有一名新成员成功加入。

### 4.4 权限平等与私用模式

产品中的 CEO、董事长等都是展示称呼，不是安全角色。两个人都有发起申请、管理本人资料、停止接收提醒和退出空间的权利；不允许一方阻止另一方退出。

生产首版采用服务端白名单，仅允许两名已配置用户读写私人空间。开发初始化阶段可通过受控流程取得本人标识并登记，完成后关闭初始化入口；不使用“第一个打开的人就是管理员”的机制。

平台审核使用隔离演示空间或明确标注的演示数据，不给审核访问者读取真实情侣资料的权限。演示不隐藏功能、不伪装审批通过状态；具体审核可接受的测试方式需要在后台确认。

## 05｜待办模块：业务规则

### 5.1 用户目标

把“记得订票”“周末一起收拾房间”“帮忙买牛奶”变成可看见、可确认完成的共同事项。

| 字段 | 首版规则 |
| --- | --- |
| 标题 | 必填，去除首尾空白，1–80 个字符 |
| 说明 | 选填，最多 1,000 个字符，纯文本 |
| 负责人 | 自己、对方、一起；“一起”以 assignee_id 为空表达 |
| 截止时间 | 选填；支持具体时间，按空间时区展示 |
| 优先级 | 普通 / 重要，避免过多等级 |
| 状态 | todo / doing / done / cancelled |
| 完成人 | 完成时由服务端记录 completed_by 与 completed_at |
| 版本 | version 用于识别并发修改 |

### 5.2 状态与操作权限

正常路径为 todo → doing → done，也允许 todo 直接完成。done 可重开为 todo；cancelled 为终态，需另建新任务。逾期是按截止时间与当前状态实时计算的展示标签，不新增一个会和完成状态冲突的持久状态。

两人可查看、编辑共享任务的标题、说明、负责人和截止时间，变更需记录操作者。指定负责人负责开始、完成、重开；“一起”任务两人任一方均可完成。仅创建者可取消或删除本人创建的任务。取消、重开需明确操作，不能在普通编辑时任意传入状态绕过规则。

普通删除为软删除，保留恢复期；涉及个人数据删除请求时走独立的数据删除流程，而不是把软删除当成彻底删除。

### 5.3 列表与同步

默认展示未完成、与我有关的任务，逾期优先，其次按截止时间排序，没有截止时间的排后。支持切换“全部”和“已完成”。每页 20 条，上限 50 条，由服务端限制；不一次拉取整个数据库。

首版采用页面进入刷新、下拉刷新、操作成功后刷新，不承诺对方正在看的页面会瞬时改变。全部业务数据经云函数读取，不为了实时效果开放数据库。后续确有需要再单独设计受权限保护的实时订阅。

### 5.4 验收场景

A 建立指派给 B 的任务，B 进入或刷新能看到；B 完成后 A 刷新能看到真实完成状态。断网保存失败时不丢表单。重复提交同一请求只产生一条任务。A 与 B 同时修改同一版本时，后提交者收到冲突提示，而不是静默覆盖。

## 06｜审批模块：固定表单，不做流程引擎

### 6.1 产品语义

审批是双方自愿的生活互动，不授予对方对现实行为的控制权。默认按钮可用“同意安排 / 再商量”，在 OA 趣味模式下显示“批准 / 驳回”；其业务状态相同。不开启惩罚、定位取证、强制报备或不回复自动同意。

首版模板建议为约会提案、外出安排、娱乐安排、共同购买、其他事项。模板只负责预填标题与说明，不需要可视化表单设计器。

### 6.2 表单与状态

提交内容包括模板类型、标题、说明，可选开始/结束时间、希望回复时间。申请人为当前用户，审批人为同空间另一名成员，均由服务端确定。单人等待绑定时只能保存本地草稿，不能提交给不存在的审批人。

状态为 draft、pending、approved、rejected、cancelled、expired。首版 draft 仅保存在当前设备，不算已经提交的云端记录；其他状态存云端。

| 当前状态 | 允许动作 | 可操作人 |
| --- | --- | --- |
| pending | approved 或 rejected，填写一条审批意见 | 指定审批人 |
| pending | cancelled | 申请人 |
| pending 且超过 reply_deadline | expired | 服务端任务或相关接口检查 |
| approved / rejected / cancelled / expired | 只读；可复制为新申请 | 原空间成员 |

审批人不可批准自己的申请。已提交正文不可直接编辑，内容变化须撤回重提。决定提交后不可在后台界面偷偷改写，只能另发新申请。回复期限是可选字段；未设置时不会自动过期。

### 6.3 并发与下游操作

审批决定以“记录仍 pending、版本一致、操作者正确、尚未过期”为原子前提。重试不会二次审批；A 撤回与 B 批准同时发生时，只能有一个成功，另一个得到当前状态。

同意后提供“创建日程”按钮，不自动复制所有申请到日历。用户确认后创建日程，并按 source_approval_id 做幂等处理，防止连续点击生成多个相同日程。

审批消息只写入目标用户的站内收件箱；微信发送失败不能把审批结果改回待处理。首页只统计真实 pending 状态，不把草稿与已过期申请算作待办。

## 07｜日历、纪念日与愿望模块

### 7.1 共同日历

支持月历与按日列表。每条日程有标题、说明、类型、全天标记、开始和结束时间、创建者。地点先用手工文本，不接地图、不申请定位。两人可查看与编辑；仅创建者可删除，其他人可提出调整。

普通日程只做一次性安排；首版不做任意每周、每月重复规则。来自审批的日程保留来源，点开可查看原申请；审批和日程删除互不级联，避免删除一个记录误删另一个。

### 7.2 纪念日

支持公历日期、每年重复、当日提示与倒计时。农历、复杂节假日规则放到以后。二月二十九日遇非闰年，首版明确按二月二十八日提醒，并在设置说明；这属于产品约定，不是日期标准。

“在一起天数”按空间时区的日历日期计算，起始当天记为第 1 天，不直接用毫秒差除以 24 小时。设置未来起始日期时显示倒计时，不出现负数恋爱天数。

### 7.3 时区规则

空间默认建议 Asia/Shanghai，可由两人协商修改；该默认值是产品选择，不基于设备所在地推断。具体时刻存 UTC 毫秒时间戳，日期型纪念日单独存 YYYY-MM-DD。全天事件不得简单转为 UTC 零点再按本地时间显示。

创建和修改带时间的事项时显示当前空间时区；切换时区不悄悄改变已存储的绝对时刻。测试覆盖两台手机不同时区、跨日、跨年和闰年。

### 7.4 愿望清单

分类为想吃、想去、想买、想做；字段为标题、描述、分类、创建者、状态、可选目标日期。状态为 pending / planned / completed / archived。

两人均可新增、共同完善、标记实现；仅创建者可删除本人记录。标记完成时可写一句完成说明并记录完成人与日期，不要求上传照片。愿望可手动转换成任务或日程，转换需显式确认，并记录来源防止重复创建。

### 7.5 第一版的使用闭环

例如“想去看展”先进入愿望池 → 选定周末后转为日程 → 建立“买票”待办 → 完成行程后标记愿望实现。无需额外增加旅行管理、订单系统或积分商城。

## 08｜视觉规范与通知方案

### 8.1 视觉基调

以“轻办公、有温度”为方向。白色或暖灰背景，深色正文，单一低饱和强调色；红色主要用于风险或删除，不把整屏做成粉红爱心。四个 Tab 使用统一线性图标，卡片风格与按钮样式一致。

采用 8px 间距体系、清楚的标题层级、足够大的点击区域。状态不能只依赖颜色，应同时有文字或图标。适配安全区域、键盘弹起、小屏、长昵称和系统大字体。不使用遮挡操作的长开场动画。

工作台和待办列表先形成一套统一样式，再扩展其余页面；不一边做后端一边反复换完整视觉体系。首页装饰数据全部标注演示或来自实际记录。

### 8.2 必须交付的站内消息

以下动作生成站内消息：收到加入申请、绑定成功、被指派任务、任务完成、收到审批、审批决定、重要日程变更、空间归档。消息记录与相关业务写入一起完成，避免有事项却没有站内通知。

消息只由服务端根据业务事件创建。客户端不能传入任意接收人、任意模板或任意发送内容。查看消息时还须检查当前资源权限；关联记录被删除或空间权限变化后，显示“内容已不可用”，不继续展示私密预览。

### 8.3 微信订阅消息是增强，不是保证送达

官方云开发方案采用前端请求用户订阅、云函数发送的方式；一次性消息与长期订阅的适用范围不同，不应假设普通生活工具能持续无限推送。[S5][S6]

发送条件为：当前账号有合适模板、接收者主动授权且授权机会可用、消息与订阅场景一致、业务事件仍有效。A 的授权不能替代 B；设置页打开一个“提醒开关”也不等于取得平台永久推送权限。

用户拒绝或没有可用授权时，仍能保存日程、办理审批、使用全部核心功能；显示“已保存，微信提醒尚未启用”，保留站内消息与用户主动分享入口。重要事项不应仅依赖本工具提醒。

### 8.4 定时处理与重试

提醒能力验证成功后，再部署 scheduleJobs 函数，通过云端定时触发器扫描到期记录，不依赖小程序在后台常驻。[S7]

建议每 5 分钟扫描一次，按 notify_at 索引取有限批次。以事件 ID、接收人、提醒时点作为去重键，发送前再次检查业务状态与订阅状态。明确失败的临时错误可有限退避重试；结果未知的超时标记 unknown，不盲目重发，避免重复打扰。不承诺跨微信外部接口严格 exactly-once。

## 09｜技术架构与工程组织

### 9.1 架构

```text
微信小程序原生客户端
  页面 / 组件 / 表单校验 / services
                 ↓ wx.cloud.callFunction
CloudBase api 云函数
  可信身份 → 成员权限 → 参数校验 → 业务模块
                 ↓
文档型数据库
  用户 / 空间 / 邀请 / 待办 / 审批 / 日程 / 愿望 / 消息

可选定时云函数 scheduleJobs → 提醒、过期处理、清理
受控运维脚本             → 备份、恢复、部署检查
```

方案采用服务端集中处理读写，避免让前端散落数据库权限判断。一个业务 api 函数并不等于把所有代码写成一个大文件：内部按模块拆分，共享鉴权、校验、错误处理与事务辅助函数即可。

首版不需要 HTTP 网关、自建域名、Nginx、Redis、消息队列、微服务或 Kubernetes。原生云调用可避免自己搭 HTTPS 服务；但仍需完成平台上线手续。[S2]

### 9.2 推荐目录

```text
couple-oa/
  miniprogram/
    pages/             # home, items, calendar, together 等
    components/        # 表单项、状态标签、空状态
    services/          # 唯一云函数调用封装
    utils/             # 时间、格式化、错误映射
    app.ts / app.json / app.wxss
  server/
    api/
      index.ts
      modules/         # auth, couple, task, approval, event, wish
      common/          # 身份、校验、幂等、错误处理
    jobs/              # 能力验证后再部署
  shared/              # 请求/响应类型、枚举
  cloudfunctions/      # 构建输出，不手工混入源码
  scripts/             # 构建、索引、备份、恢复
  tests/               # 单元、权限、集成用例
  docs/                # 方案、接口、部署、验收记录
  project.config.json
  AGENTS.md
```

### 9.3 构建与依赖约束

前后端使用 TypeScript，但云函数部署产物为平台支持的 JavaScript。构建时把 shared 依赖编译或打包进每个部署目录；不得在云端引用未上传的本地路径。

选用开工时平台支持的 Node.js 运行时与稳定 SDK，锁定依赖版本并记录，不把示例中的旧版本号直接当成当前推荐。禁止把 API 密钥、AppSecret、导出加密密钥写进小程序或 Git；AppID、envId 是标识，不是安全凭据。

## 10｜数据模型：身份与空间

### 10.1 统一约定

集合使用下划线命名，字段统一 snake_case；不要混用 coupleId 与 couple_id。业务 ID 由服务端生成。所有已持久化业务记录包含 _id、created_at、updated_at；可编辑共享记录增加 version。时间戳统一 UTC 毫秒，日期型字段保留日期字符串。

couple_id 是数据归属字段，不是通行证。普通接口无需客户端指定空间 ID，服务端从当前成员关系解析；接口按 record_id 查到数据后仍需核对其空间与权限。

### 10.2 集合与字段

| 集合 | 核心字段 | 约束 |
| --- | --- | --- |
| users | _id, appid, openid, nickname, avatar_key, couple_id, created_at, updated_at | 一身份对应一用户；openid 仅服务端使用 |
| couples | _id, name, member_ids, created_by, start_date, timezone, status, archived_member_ids, archived_at, purge_at, version | 活跃成员最多 2；status 为 waiting / active / archived |
| invitations | _id, couple_id, token_hash, created_by, expires_at, revoked_at, consumed_at | 不保存或返回明文；只能完成一次绑定 |
| join_requests | _id, invitation_id, applicant_id, status, expires_at, decided_at | pending / approved / rejected / expired；未获准不能读空间 |
| request_dedup | _id, actor_id, action, request_id, payload_hash, result_ref, expires_at | 防重复写入；同请求号不同内容应拒绝 |

users 可使用 appid 与 openid 的确定性哈希生成 _id，利用文档主键防止并发创建重复账号；哈希 ID 本身同样不能作为授权依据。若采用随机用户 ID，必须另外实现等价的身份唯一性保障。

### 10.3 绑定事务及索引

确认加入时使用已知的用户、空间、邀请、申请记录 ID 进行事务读写，避免依赖未经验证的跨集合复杂查询能力。事务提交前核对相关版本和状态；重试重新读取最新状态。

按实际访问方式建立索引：invitations.token_hash；join_requests 的 invitation_id + status；用户身份查找约束；request_dedup 的 expires_at。过期索引只是查询优化，不能把后台清理当作授权有效性的判断；每次使用邀请码都要即时比较 expires_at。

### 10.4 最小化系统复杂度

不增加通用组织表、部门树、权限角色系统或可配置流程定义表。两人空间无需设计成企业 SaaS，但仍保留明确的数据边界，便于测试跨空间越权和未来迁移。

## 11｜数据模型：业务与消息

| 集合 | 核心业务字段 | 关键规则 |
| --- | --- | --- |
| tasks | couple_id, title, description, creator_id, assignee_id, due_at, priority, status, completed_by, completed_at, version, deleted_at | 负责人必须是当前空间成员或共同任务 |
| approvals | couple_id, template_key, title, content, applicant_id, approver_id, start_at, end_at, reply_deadline, status, decision_comment, decided_at, version | 申请人与审批人不同；已提交正文不可直接改 |
| events | couple_id, title, description, type, all_day, start_at, end_at, date_start, date_end, creator_id, source_approval_id, source_wish_id, version, deleted_at | 全天用日期字段；具体时间用时间戳；两类互斥 |
| anniversaries | couple_id, title, original_date, recurrence, leap_day_policy, creator_id, version, deleted_at | recurrence 首版仅 yearly；不存固定倒计时 |
| wishes | couple_id, title, description, category, status, target_date, creator_id, completed_by, completed_at, version, deleted_at | 完成后记录真实操作人 |
| notifications | couple_id, receiver_id, event_key, type, resource_type, resource_id, read_at, created_at | 每人独立已读；对外预览不泄露正文 |
| audit_logs | couple_id, actor_id, action, resource_type, resource_id, request_id, changed_fields, created_at | 记录变更类型，不复制整段私密正文 |
| notification_jobs | event_key, receiver_id, resource_id, notify_at, status, attempt_count, last_error_code | 订阅能力启用后才建立；去重与重试分开 |

### 11.1 示例任务

```json
{
  "_id": "task_example",
  "couple_id": "cp_example",
  "title": "订周末的电影票",
  "description": "选两个人都方便的场次",
  "creator_id": "user_a",
  "assignee_id": "user_b",
  "due_at": null,
  "priority": "normal",
  "status": "todo",
  "completed_by": null,
  "completed_at": null,
  "version": 1,
  "deleted_at": null
}
```

该示例省略统一时间字段；所有示例 ID 均为占位值，不用于真实身份或邀请。

### 11.2 查询索引建议

tasks 按 couple_id + status + due_at 查询；approvals 按 couple_id + approver_id + status + created_at 查询；events 按 couple_id 与起始时间/日期分别查询；wishes 按 couple_id + status + created_at 查询；notifications 按 receiver_id + read_at + created_at 查询。

所有列表均使用固定字段白名单和分页，不能让客户端传入任意数据库过滤表达式或集合名。事件时间区间查询还需覆盖跨日事件，不只是筛选“今天开始”的日程。最终索引须通过实际查询、控制台限制和慢查询情况验证。

## 12｜接口契约与错误处理

### 12.1 统一调用形式

以下是待实现的项目接口约定，不是微信平台自带 API。

```json
{
  "action": "task.create",
  "request_id": "客户端生成并在重试时复用的随机ID",
  "payload": {"title": "订电影票", "assignee_id": "user_b", "due_at": null}
}
```

成功返回 ok=true、data、request_id、server_time；失败返回 ok=false、error.code、error.message、request_id。不要把数据库异常堆栈、另一用户 OPENID 或完整请求内容返回到页面。

### 12.2 动作目录

| 领域 | 主要动作 | 说明 |
| --- | --- | --- |
| 身份 | auth.bootstrap, user.updateProfile | 识别本人，更新本人可编辑资料 |
| 空间 | couple.create, couple.get, couple.update | 成员数组禁止经普通编辑修改 |
| 邀请 | invite.create, invite.revoke, join.request, join.confirm, join.reject | 生成邀请、申请加入、确认与拒绝 |
| 工作台 | dashboard.get | 统计限定范围，并标注时间 |
| 待办 | task.list, task.get, task.create, task.update, task.transition, task.delete | 字段修改与状态转换分开 |
| 审批 | approval.list, approval.get, approval.submit, approval.decide, approval.cancel | 正文与决定不可任意覆盖 |
| 日程 | event.list, event.get, event.create, event.update, event.delete | 转换来源也经过服务端校验 |
| 纪念日 | anniversary.list, anniversary.create, anniversary.update, anniversary.delete | 返回计算后的下一次日期 |
| 愿望 | wish.list, wish.get, wish.create, wish.update, wish.complete, wish.delete | 标记完成有独立动作 |
| 消息 | notification.list, notification.markRead | 只能操作本人收件箱 |
| 数据 | data.export, couple.archive, account.delete | 高风险流程，禁止普通更新绕过 |

### 12.3 并发与幂等

写操作携带 request_id，服务端以“调用者 + 动作 + 请求号”识别重试并校验 payload_hash。同号异内容返回冲突；业务记录与幂等结果在同一事务写入。

更新携带 expected_version，匹配时写入 version + 1，否则返回 VERSION_CONFLICT 并要求重读确认。禁用按钮不能替代服务端并发保护。

幂等记录建议保留 7 天，审批决定与绑定仍保留永久状态约束。未知结果应按请求号查询或复用原请求重试，不生成新号重复创建。

### 12.4 统一错误码

至少定义 UNAUTHENTICATED、FORBIDDEN、NOT_FOUND、VALIDATION_ERROR、INVITE_INVALID、ALREADY_BOUND、COUPLE_FULL、INVALID_STATE、VERSION_CONFLICT、RATE_LIMITED、SERVICE_UNAVAILABLE。对无权访问的其他空间记录可统一返回 NOT_FOUND，避免泄露记录是否存在。

## 13｜安全与隐私边界

### 13.1 服务端授权是必需项

每个业务动作依次完成：校验可信调用来源 → 解析当前用户 → 读取最新空间状态 → 校验成员关系 → 校验记录归属 → 校验动作权限 → 校验参数与状态 → 执行写入。

数据库集合默认禁止客户端直接读写，业务经受控云函数访问。CloudBase 服务端权限与客户端规则不同，云函数中仍须自行执行严格授权，不能以为配置了规则就万事大吉。[S4]

对于更新、删除、审批、导出等接口，必须在服务端查到真实资源后核对 couple_id；不接受“前端已经在同一个页面”作为安全理由。不允许客户端修改 creator_id、member_ids、receiver_id 或本人 OPENID。

### 13.2 平台管理员仍然是信任边界

业务界面只对授权的两个人开放，但本方案**不是端到端加密系统**。云账号拥有者、被授权的控制台管理员及相应服务端身份可能具有数据访问能力，不能宣传为“连开发者也看不到”。云存储官方权限文档也明确说明了控制台和服务端的权限边界。[S8]

云账号启用多因素验证，限制协作者权限，不将控制台账号交给另一方作为产品使用方式。需要真正抵抗管理员读取时，必须重新设计端到端加密、密钥恢复与多设备同步，不在本版附带承诺。

### 13.3 隐私最小化

首版只收集服务必要的微信身份标识、用户主动填写的昵称、空间关系和业务记录。不要申请通讯录、位置、手机号、录音或后台定位权限；头像先用预设图标。

完善《小程序用户隐私保护指引》并与实际接口保持一致。涉及微信隐私接口时，应按照平台的声明及授权要求处理；拒绝非必需权限不应阻断无关功能。[S9]

日志默认不记录任务正文、审批详情、邀请码明文、完整 OPENID 和敏感下载链接。排错主要依赖 request_id、错误码、动作类型、资源 ID 与耗时。

### 13.4 后续文件上传准入条件

相册不进入首版。以后增加照片前，须完成私有存储、服务端文件归属校验、短时授权访问、大小与格式限制、孤立文件清理和删除传播测试；仅把图片放到 couple_id 目录不等于已隔离。[S8]

所有微信分享卡片、订阅消息预览默认使用“你有一项共同事务待处理”等中性文字。具体内容进入小程序并鉴权后再显示，避免锁屏或群分享泄露隐私。

## 14｜导出、解绑、删除与备份

### 14.1 导出

提供“导出我的资料及当前有权访问的共享记录”，首版导出 JSON。导出内容不包含 OPENID、邀请码哈希、运维密钥和系统日志。授权检查必须与普通查询一样执行，不能开放一个导出全库接口。

文本规模较小时，由分页接口读取允许导出的数据，再在小程序本地生成文件，采用实际支持的用户主动保存/分享能力；交付前须完成真机验证。输出应有 schema_version、exported_at、空间时区和各集合数据。超过预设大小时分批处理并给出进度，不无限扩大云函数响应体。

### 14.2 解绑不需要对方批准

任一方选择结束共享，确认后立即归档当前空间，停止新增共享、审批决定和对外提醒，撤销未完成邀请。两人的活跃 couple_id 解除，原成员快照用于受限历史资料处理。

建议归档资料保留 30 天只读窗口，原两名成员可导出此前有权访问的共享记录。新空间始终使用新的 couple_id；任何新加入者都不得继承旧空间历史。此 30 天为产品默认策略，不是法律规定。

个人数据删除请求不以对方同意或等待归档期满为前提。共享内容涉及双方权利时按具体内容处理，不能把“全部清空对方的记录”当作普通单方按钮。已被另一方主动导出、截屏或保存在设备上的副本不能承诺远程收回。

### 14.3 账号删除

显式说明删除范围；校验当前身份后退出共享、撤销提醒授权记录、清除本人资料，并依照既定规则删除或去标识化本人贡献的记录。涉及共同事项时保护另一方仍有权保存的数据。

列出删除执行状态与预计保留范围，不仅改一个 deleted=true 就显示“彻底删除”。技术日志、备份等保留策略需写入隐私说明；涉及法定保留义务时单独处理，实际对外运营前复核。

### 14.4 备份与恢复

建立应用级备份，不假设最低套餐自带可用的时间点回档。当前价格表中免费体验版、个人版未列出数据回档能力，应结合购买环境确认。[S3]

建议由受控定时云函数每天备份业务集合，版本变更和批量删除前另用受控脚本备份。定时备份入口不得接受普通客户端触发；运维脚本使用最小权限云身份，不需要再购买常驻服务器。备份加密保存到受控位置，保留最近 7 个日备份与 4 个周备份，并设置最长 30 天留存；不以同一数据库内复制一份代替备份。

至少每周保留一份独立于生产环境的受控副本。不要把密钥和密文放在同一个可公开下载的位置。涉及删除的数据维护清理记录，恢复时先应用删除清单，避免让已删除内容重新出现。

发布前必须做一次真实恢复演练：备份测试空间 → 删除若干测试记录 → 恢复至隔离环境 → 核对数量、关联和权限。未做过恢复测试不能标记“备份完成”。

## 15｜环境、部署与上线

### 15.1 环境策略

开始时可以只创建一个开发环境，里面仅放测试数据。正式承载私人数据前分离 dev 和 prod；两个环境可能分别计费，不能默认为都免费。若预算只允许一个正式环境，日常单元测试用本地模拟，云端破坏性测试必须迁到临时测试环境，不在生产执行。

不同环境的配置显式记录；正式包只引用生产 envId。上传到微信的体验版和正式版是代码发布状态，不等于天然拥有两套数据库。

### 15.2 部署顺序

| 步骤 | 交付物 / 核验 |
| --- | --- |
| 1. 注册与能力检查 | AppID、类目、云环境类型、费用及有效期记录 |
| 2. 本地工程 | 可导入开发者工具；TypeScript 构建成功 |
| 3. 云环境初始化 | 创建集合、索引、权限；部署最小身份函数 |
| 4. 双真机身份验证 | 两个可信用户标识不同，能关联正确环境 |
| 5. 业务部署 | 云函数先向后兼容部署，再上传客户端 |
| 6. 体验验证 | 核心流程、越权、断网、重复请求与并发通过 |
| 7. 数据与配置 | 生产白名单、备份、告警、实际隐私指引准备完成 |
| 8. 平台手续 | 备案、适用认证、类目及代码审核按当前后台办理 |
| 9. 发布与回归 | 正式版双端使用验证，确认不是开发环境假成功 |

### 15.3 上线材料与访问

准备真实名称、图标、功能描述、合适类目、隐私保护指引、所需主体材料，以及明确的审核测试路径。不把“小程序备案”与“购买域名办理网站备案”混为一谈；小程序通常通过其运行平台办理相应备案手续。[S10]

建议产品说明：“供已建立关系的两名用户，通过邀请共同管理待办、日程、愿望和生活提案。”该表述应与实际功能一致，不包装成陌生人交友或婚恋撮合服务。

体验版只用于平台允许范围内的开发测试，不将其视为永久规避备案、审核或费用的正式分发渠道。个人主体能否承载全部实际功能，须以类目和审核结论为准，本方案不保证审核通过。

### 15.4 回滚与维护

每次发布保存代码版本、云函数构建产物和数据结构版本。先增加兼容字段，再发布使用新字段的客户端；不在同一次发布里删旧字段并假设所有用户已更新。云函数与客户端均应有回退说明。

每周查看一次错误与用量；每月检查续费、备份、删除任务和依赖风险。监控不记录私密正文。问题优先级为：越权或数据丢失 → 主流程不可用 → 错误提醒 → UI 细节。

## 16｜开发任务拆分与阶段门槛

以下按成果划分，不按“让 AI 一次生成全部代码”执行。每个阶段完成可验证交付后，再开启下一阶段；平台审核与开通等待不计作编码任务。

| 阶段 | 开发内容 | 进入下一阶段的门槛 |
| --- | --- | --- |
| P0 能力验证 | 账号、类目、env、身份最小调用、订阅模板探测 | 双真机识别成功；费用、可用能力已记录 |
| P1 工程骨架 | 原生 TS 工程、四 Tab、统一样式、API 封装、错误处理 | 可编译导入；没有假接口成功提示 |
| P2 身份与绑定 | 用户、空间、邀请、加入确认、白名单、授权 | 两人绑定；第三人和并发加入均正确受限 |
| P3 待办闭环 | 任务增查改、状态、分页、幂等、冲突、站内消息 | 双方真实协作；断网与重复提交通过 |
| P4 审批闭环 | 固定模板、提交、决定、撤回、过期、记录 | 撤回/批准并发不会双成功；权限正确 |
| P5 完整首版 | 日历、纪念日、愿望、工作台聚合 | 时间规则与来源转换通过测试 |
| P6 可靠性 | 导出、解绑、删除、备份恢复、成本限制 | 数据导出可读；旧空间不泄露给新成员 |
| P7 上线交付 | 隐私指引、审核资料、正式配置、双端回归 | 所有必测项通过；平台手续完成 |

### 16.1 订阅消息的独立决策

能力探测在 P0 进行，接入可以在 P5–P6 并行。若后台没有合适模板，交付记录写明原因，关闭对应入口，保留站内消息与主动分享。不得伪造模板 ID、拿无关模板凑功能或使用非官方手段自动骚扰微信联系人。

### 16.2 每阶段必须留下的内容

每个阶段都提交可运行代码、变更说明、需要手工配置的步骤、测试结果与已知限制。云端部署和真机验证未执行时，明确写“未验证”，不能把本地单元测试通过等同于部署成功。

### 16.3 冻结范围

P2–P4 期间不加入 AI、相册、通用后台或复杂组件系统。新增想法只放入 backlog。先确认两个人愿意连续使用，再决定扩展；不能为了“一次架构到位”推迟基本协作功能。

## 17｜验收用例与交付定义

### 17.1 必测用例

| 编号 | 场景 | 通过标准 |
| --- | --- | --- |
| A01 | 不同微信身份登录 | 各自得到正确 user_id，不靠前端伪造 |
| A02 | 邀请申请但未确认 | 申请人看不到空间业务数据 |
| A03 | 两人同时尝试加入 | 空间人数不超过 2，失败方无残留绑定 |
| A04 | 过期、撤销、重复邀请码 | 均不可用于新绑定 |
| A05 | 伪造 openid / couple_id | 无法读取或写入其他用户数据 |
| A06 | 前端直接读写集合 | 被权限规则拒绝 |
| A07 | 重复创建任务 | 相同请求号只生成一条 |
| A08 | 双人并发编辑 | 一方成功，另一方收到版本冲突 |
| A09 | 完成与重开任务 | 操作者和时间真实记录，逾期显示正确 |
| A10 | 申请人自行批准 | 被服务端拒绝 |
| A11 | 撤回与批准并发 | 只有一个终态成功 |
| A12 | 修改已决定审批 | 不可静默改写正文和决定 |
| A13 | 订阅拒绝 / 发送失败 | 核心事项仍保存成功；站内消息可用 |
| A14 | 定时任务重复执行 | 不重复创建提醒任务；未知结果不盲目重发 |
| A15 | 全天、跨日、闰年日程 | 显示日期与约定规则一致 |
| A16 | 退出后重新配对 | 新成员不能获得旧空间任何资料 |
| A17 | 越权消息与导出 | 不能读取他人收件箱或批量导出全库 |
| A18 | 删除与恢复备份 | 删除清单仍被执行，已删数据不复活 |
| A19 | 断网、超时与服务异常 | 不丢输入、不假成功、不重复写入 |
| A20 | iOS / Android 正式包 | 核心操作通过；未测试机型明确记录 |

A05、A06、A16、A17 任意一项失败，不得上线真实私人数据。所有涉及后端的关键用例必须至少在一个真实云环境验证，不能全部依赖模拟接口。

### 17.2 定义完成

功能完成必须同时满足：正常流程可用、异常处理清楚、服务端权限正确、类型和测试通过、部署步骤可复现、真实设备有验证记录。首页漂亮或演示数据齐全不等于业务完成。

### 17.3 小规模使用验证

正式使用前做一轮两人真实试用：至少完成一次创建与绑定、五条共同任务、两次相反方向的审批、一次日程修改、一次愿望转换和一次导出。数字是本项目验收样本，不是增长指标。

重点观察哪里会让人不想打开、是否有重复填写、提醒是否打扰、审批文案是否造成负担。没有使用价值的功能删除或简化，不靠更多模块掩盖问题。

## 18｜交给 Codex 的执行说明

### 18.1 仓库级约束建议写入 AGENTS.md

```text
项目：情侣 OA 微信小程序；两人自用，不自行购买服务器。
技术：原生 WXML/WXSS/TypeScript + CloudBase 文档数据库和云函数。
只沿用经验证的 wx.cloud 原生身份链路，不混入其他自定义登录方案。

先阅读 docs 中的产品方案、接口约定与当前阶段说明。
先检查已有代码和配置，再修改；不推倒重来、不擅自扩范围。
只实现本次阶段，未开发页面不得伪装成已成功的功能。
所有业务读写走受控云函数，身份从可信服务端上下文取得。
客户端传入的用户ID、空间ID、状态和接收人一律不直接信任。
关键写入实现权限、状态校验、事务/条件更新、幂等与版本冲突。
不引入微服务、Redis、MQ、AI、相册或通用流程引擎。
密钥不进代码库；不要用生产资料做测试。
涉及云账号开通、授权、付费和发布的操作须交由我明确执行或确认。

交付必须列出：改动文件、运行方式、手工配置、测试结果、已知限制。
没有运行过的测试标为未验证，不声称已经部署或通过平台审核。
```

### 18.2 第一条开发任务

```text
请先实施情侣 OA 的 P0 和最小 P1，不做全部产品。

1. 检查目录中是否已有项目；已有代码优先复用。
2. 建立可导入微信开发者工具的原生 TypeScript 工程。
3. 提供环境配置示例，不写真实密钥或替我购买资源。
4. 创建最小云函数，使用可信上下文识别当前调用者。
5. 前端展示真实调用成功、失败和未配置环境三种状态。
6. 建立统一调用封装、错误格式及基本测试脚本。
7. 提供两台真机验证不同身份的操作说明。
8. 输出待手工确认清单：AppID、环境关联、类目、费用、消息模板。

不要实现复杂 UI、绑定、待办、审批、AI 或相册。
完成后报告哪些已实际运行、哪些仍需我在微信和云后台操作。
```

### 18.3 第二条开发任务

```text
在已通过身份验证的项目上，只实施 P2：创建空间与双人绑定。
实现邀请生成、加入申请、邀请发起人确认，以及对应服务端权限。
邀请码安全随机、仅存哈希、24小时有效、可撤销；确认成功后作废。
使用事务保证空间最多两名活跃成员，用户不能同时占用两个活跃空间。
未获准申请人、第三人、伪造couple_id和过期邀请均不能访问私人数据。
添加重复请求与并发加入测试；写清生产白名单初始化步骤。
不添加待办、审批或其他业务，完成后提交可复现验证步骤与结果。
```

### 18.4 项目脚本约定

后续工程至少提供 typecheck、test、build、部署前检查和受控备份恢复脚本。常规检查命令可统一约定为 npm run typecheck、npm test、npm run build。它们是待实现的工程要求，不表示当前已经存在或运行成功。

备份、恢复和数据清理脚本须要求显式指定环境；发现目标为生产时再次确认，不允许默认无提示覆盖。自动生成的脚本同样接受安全审查。

## 19｜风险、后续扩展与最终执行建议

### 19.1 优先处理的风险

最大风险不是两个人的并发量，而是平台能力不匹配、免费环境到期、错误权限、丢失数据，以及项目范围不断扩张。对应对策已经纳入 P0 能力检查、正式费用预算、统一服务端授权、恢复演练和阶段门槛。

提醒能力受平台授权及实际模板限制；审核与认证费用未在当前账号后台验证。将这些项目列为上线前检查项，而不是在方案里承诺一定开放或一定通过。

### 19.2 扩展顺序

稳定使用后，优先改善表单便捷性与提醒体验，再考虑简单记账、少量图片、共同回顾。引入记账时金额用整数分、先只做支出记录，不做收款与转账。引入图片前先满足私有文件访问准入条件。

AI 助理应放在有实际数据和明确需求之后；默认不把私密记录发送给第三方模型。接入前单独确认双方授权、发送范围、数据保留、费用和当时的上线要求。AI 可以建议整理事项，不直接替任何一方批准申请、删除记录或代表另一人发言。

### 19.3 当前最优执行顺序

先验证小程序账号与云环境能力，再交给 Codex 做可信身份和双人绑定。第一条真正的业务闭环是“你建一条任务，对方处理，你看到结果”；第二条是“双方都能发起并处理轻量申请”。

完成这两条后，再把日历、纪念日和愿望叠上去。生产使用前补齐导出、解绑、备份、权限和平台手续，不把这些当作永远后补的非功能项。

**最终边界：做一个能长期、安心地使用的双人生活协作工具，不做只有两名用户的企业级信息化工程。**

## 20｜核验来源与使用说明

下列为本方案采用的公开来源。平台事实与价格核验日期为 2026-09-15。产品字段、权限策略、预算预留和开发阶段属于本方案设计，不是微信或腾讯要求。部分微信文档页未能直接读取，相关内容采用腾讯云官方说明及平台服务商的备案指引交叉参考；具体类目、模板和认证费用仍以实际账号后台为准。

**[S1] 腾讯云 CloudBase 文档中心：产品能力。**
https://docs.cloudbase.net/
支持数据库、存储、身份认证、云函数及云托管等托管能力的说明。

**[S2] CloudBase：在微信小程序中调用云函数。**
https://docs.cloudbase.net/recipes/add-cloud-function-wechat-miniprogram
原生 wx.cloud / wx-server-sdk 路径、微信身份上下文、环境关联。

**[S3] 腾讯云：CloudBase 资源点价格文档。**
https://cloud.tencent.com/document/product/876/127357
免费体验与发布后到期规则、个人版限时价格、配额、数据回档及计量延迟；购买时再次核对。

**[S4] CloudBase：权限控制。**
https://docs.cloudbase.net/authentication-v2/auth/auth-control
云函数服务端仍须严格实现授权，不将客户端规则等同于服务端业务权限。

**[S5] CloudBase：用云函数给微信小程序发订阅消息。**
https://docs.cloudbase.net/recipes/add-subscribe-message-cloud-function
前端订阅授权与云函数发送的官方实现路径。

**[S6] 腾讯云云开发团队：用云开发实现小程序订阅消息配置。**
https://www.cnblogs.com/CloudBase/articles/11690317.html
团队历史说明，用于解释一次性订阅与长期订阅的区别；不据此推定 2026 年具体账号资格。

**[S7] CloudBase：云函数定时触发器。**
https://docs.cloudbase.net/recipes/schedule-cloud-function-cron-job
云端定时任务实现路径。

**[S8] CloudBase：云存储安全规则。**
https://docs.cloudbase.net/storage/security-rules
私有文件访问与控制台、服务端的权限边界。

**[S9] 腾讯云：小程序隐私保护指引适配说明。**
https://cloud.tencent.com/document/product/1301/97930
涉及微信隐私接口时的声明和授权要求；其中微搭自动适配部分不直接套用于原生项目。

**[S10] 世纪互联 Azure：ICP备案常见问题。**
https://www.azure.cn/support/icp/icp-faq/
小程序通过运行平台履行备案手续等说明，结合微信实际后台办理。

**官方操作入口名称：**微信公众平台、微信开发者工具、腾讯云 CloudBase 控制台、工信部 ICP/IP 地址/域名信息备案管理系统。注册、实名认证、认证支付、备案提交与正式发布由账号持有人确认执行。
