# 实施记录

## 2026-09-16：基线与私钥保护

- 在任何新功能开发前先将 `private.*.key`、原始 ZIP、备份/导出目录加入 `.gitignore`，建立 Git 基线提交 `f75f895`。
- AppID 为 `wx3efc253d50e263b6`；私钥仅供本机 `miniprogram-ci` 读取，没有进入 Git 或命令输出。
- 本项目只使用局部 Node 依赖和 lockfile，未改动 Conda base。

## 2026-09-16：P2 身份、空间与绑定

- 将微信身份和业务用户拆成 `identities`/`users`：身份文档键是完整 `SHA-256(appid + openid)`，业务 `user_id` 随机生成。删号会同时删除两者，重新进入会获得新业务身份；同时保留了早期截断键的一次性迁移。
- 实现用户资料、创建空间、10 位无混淆字符邀请码、加入申请、确认/拒绝/撤销/过期。
- 所有写操作统一进入服务端事务和 `request_dedup`；加入确认在同一事务中处理空间、用户、申请和邀请。
- 难点：白名单 OpenID 只能从可信云端上下文取得。保留了服务端临时初始化令牌，两个测试账号登记后必须立即删除令牌并启用白名单。

## 2026-09-16：P3–P5 业务与界面

- 完成工作台聚合、待办创建/编辑/状态迁移/取消/软删除、审批模板/决定/撤回/复制重提/转日程、站内消息、跨日日历、纪念日、心愿转待办/日程去重。日程、纪念日和心愿均提供完整维护入口。
- 客户端无直连数据库代码；服务端自行确定空间、收件人和消息摘要，不信任前端传入的 OpenID/user_id/couple_id。
- 从 43MB 资产包中筛出运行时需要的纸张、装饰和图标，改为英文名并按真实尺寸重采样压缩；后续逐页还原加入生成照片后，最终源码包为 1789.5 KiB，仍低于 2 MiB 主包限制。
- 视觉难点：参考图含整页手账纹理和真人照片。实现时使用分层纸张素材、预设双人头像和 WeUI 真实图标，没有整页平铺参考图，也没有申请相册权限。
- 开发者工具的账号 access token 过期，导致 IDE 的“构建 npm”请求失败。解决方式是锁定 WeUI 版本、只收录 icon 组件及其运行时，并用 `miniprogram-ci` 成功编译 67 个文件。

## 2026-09-16：P6–P7 可靠性与交付

- 分页导出逐集合拉取 50 条，生成含 `schema_version`、导出时间和时区的 JSON；服务端剥离 `_openid`/`openid`/`token_hash`。
- 解绑会立即归档并停止写入，双方用户档案保留 30 天只读导出引用；定时函数到期后分批清理。
- 删号前归档当前空间，然后将共同记录中的个人 ID 替换为不可逆匿名标识，删除个人收件箱、身份映射和用户档案。
- 新增显式 `--env` 的集合/索引/策略初始化、云函数部署、逐集合备份、时点恢复和清理脚本。生产写操作需再次传入精确环境 ID。
- 自动化验证：TypeScript 客户端/服务端类型检查、17 项领域单元测试、资产重建、云函数 esbuild、包体积和微信 CI 真实编译全部通过。当前小程序源码包 1789.5 KiB，生产依赖 `npm audit --omit=dev` 为 0 漏洞；开发工具链有旧版转递依赖告警，未使用 `--force` 破坏式升级。
- 用 `miniprogram-automator` 在微信开发者工具中抓取 6 张演示截图，并将待办/申请/日历/我们与四张参考图合并为 `design-qa-artifacts/comparison.png` 进行对照。

## 尚未能在本机完成的外部检查

- CloudBase 开发环境尚未由账号持有人创建，因此集合、索引、真实事务、白名单和云函数尚未部署。
- 开发者工具云访问令牌过期，需用户扫码重新登录。
- 代码上传 IP 白名单尚未确认；在开发云环境配置完成前不上传一个只有演示数据的伪体验版。
- iOS/Android 双账号真机 A01–A20 需环境完成后执行。

## 2026-09-16：四张参考图逐页重建与最终视觉 QA

- 用户确认初版“同风格”不足以验收后，按四张参考图的实际构图重建待办、审批、日历和我们页，而不是继续做局部换色。业务能力和云函数契约保持不变，只重排演示态首屏并保留真实数据态入口。
- 从用户提供的资产与参考图中拆出单个业务图标、头像、纸张和装饰素材；未把整张参考页面放入运行包。运行素材统一英文名、按显示尺寸压缩。
- 使用内置图像生成能力补齐 5 张缺失的项目专属照片，提示词方向分别为：暖色城市夕阳下的情侣背影、咖啡桌上相握的双手、自然光下的家常锅物、暖色户外情侣与金毛犬、夕阳前双手比心；共同约束为自然写实摄影、奶油/蜜桃/鼠尾草配色、无文字、无水印、无边框。落盘为 `miniprogram/assets/photo-sunset.jpg`、`photo-hands.jpg`、`photo-meal.jpg`、`photo-dog.jpg`、`photo-heart.jpg`。
- 难点一是源图包含展示用手机外壳，不能直接与模拟器全屏比较。最终把参考图裁切到应用内容区 `781 × 1608`，再与 `688 × 1486` 模拟器截图归一化到相同画布。
- 难点二是四页首屏内容密度不同。通过三轮实机截图修正安全区、待办 6 条/审批 4 条的完整露出、日历月视图与今日区间距，以及“我们”页英雄区和双栏卡片高度。
- 最终证据在项目根目录 `design-qa.md`，对照图由 `scripts/compose-design-qa.mjs` 生成；待办、审批、日历和我们四页均无 P0/P1/P2 视觉差异，结论为 `passed`。
