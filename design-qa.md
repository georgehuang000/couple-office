# 两人事务所视觉还原 QA

## 验收范围与环境

- 源设计：`00_页面参考图` 中的待办、申请、日历、我们四张 `941 × 1672` PNG。
- 实现截图：微信开发者工具模拟器中的真实小程序运行结果，视口 `688 × 1486`，由 `miniprogram-automator` 在只读演示模式下生成。
- 归一化规则：参考图去除展示用手机外壳，裁切 `{ left: 80, top: 28, width: 781, height: 1608 }`；参考与实现均缩放至 `500 × 900` 后进行全页对照。
- 导航状态：小程序自定义导航头、自定义四项 TabBar；微信系统状态栏与胶囊保持平台原生渲染。

## 证据

- 全页四组对照：`design-qa-artifacts/comparison.png`
- 放大对照：`design-qa-artifacts/focus-03-items.png`
- 放大对照：`design-qa-artifacts/focus-03b-approvals.png`
- 放大对照：`design-qa-artifacts/focus-04-calendar.png`
- 放大对照：`design-qa-artifacts/focus-05-us.png`
- 实机截图：`design-qa-artifacts/03-items.png`、`03b-approvals.png`、`04-calendar.png`、`05-us.png`

上述证据目录已从 Git 和小程序发布包排除；可运行 `npm run design:qa:capture && npm run design:qa:compose` 重建。

## 五项逐面核对

| 检查面 | 结果 | 结论 |
| --- | --- | --- |
| 字体与层级 | 品牌名、手写标题、正文、日期、状态标签的字号和深浅层级一致；系统字体栅格化有平台差异 | 通过 |
| 布局与间距 | 四页首屏块级位置、卡片底线、双栏比例及底部导航对齐；待办 6 条、审批 4 条完整呈现 | 通过 |
| 颜色与材质 | 奶油纸张底、鼠尾草绿、桃粉强调色、撕纸阴影和植物拼贴一致 | 通过 |
| 图片与装饰 | 照片框、胶带、枝叶、业务图标及相册缩略图的位置和裁切匹配；缺失照片用同主题生成素材替代 | 通过 |
| 文案与状态 | 页面标题、引导语、示例任务/申请/日程/纪念日/愿望及只读演示状态完整 | 通过 |

## 比对与修复记录

1. 初版仅复用了暖色手账风格，页面结构与参考图差异明显，判定不通过。
2. 重建四页 WXML/WXSS，补齐照片、卡片、图标、手写注释及真实首屏数据；统一裁切参考图的应用内容区域后重新比对。
3. 修复安全区、待办/审批首屏密度、日历上下区间距和“我们”页双栏高度；演示态隐藏真实账号管理区。
4. 最终重新编译并抓取 6 个运行状态，未发现小程序运行时异常；全页与放大证据中无 P0、P1 或 P2 视觉问题。

允许保留的非缺陷差异：系统时间、信号/电量、微信胶囊的原生渲染，以及生成照片中人物的具体外观。这些不改变应用布局、交互或视觉语言。

final result: passed
