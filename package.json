{
  "name": "couple-oa",
  "version": "0.1.0",
  "private": true,
  "description": "情侣 OA 微信小程序 P0/P1",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc -p tsconfig.json",
    "test": "node --test tests/*.test.js",
    "predeploy:check": "npm run typecheck && npm test"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
